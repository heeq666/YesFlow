import CustomNode from '../components/CustomNode';
import FloatingEdge from '../components/FloatingEdge';

export const nodeTypes = { task: CustomNode };
export const edgeTypes = { floating: FloatingEdge };
